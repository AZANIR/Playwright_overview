# Навчальний курс: Playwright — огляд, конфіг, Browser/Context/Page та UI mode

Комплексний навчальний посібник для студентів курсу «Автомейшен тестери» з використанням Playwright. Курс охоплює три теми: огляд та встановлення Playwright, конфігураційний файл та просунута конфігурація, Browser/Context/Page та UI mode.

## Про курс

Цей репозиторій містить навчальний матеріал для вивчення Playwright. Кожен модуль включає:

- **Теоретичні пояснення** — README з описом теми
- **Практичні робочі приклади** — код з детальними коментарями українською
- **Робочий тестовий фреймворк** — один Playwright-проєкт у `examples/test-frameworks/playwright/` з тестами по всіх трьох модулях

## Готові робочі приклади

Проєкт містить **повноцінний Playwright-фреймворк** у папці `examples/test-frameworks/playwright/`:

- Всі тести працюють з [playwright.dev](https://playwright.dev/)
- Тести містять детальні коментарі українською
- Готовий до запуску: `npm install && npx playwright install && npm run test`
- Приклади: базові тести, кроки, хуки, конфіг, projects, фікстури page/context/browser, UI mode

## Зміст курсу

### Модуль 01: Playwright overview & installation

**Огляд фреймворку та встановлення**

- Що таке Playwright: кросбраузерність, автоматичне очікування, трасування
- Встановлення: `npm init playwright@latest`, браузери
- Запуск тестів: `npx playwright test`, `npx playwright show-report`
- Структура тестів: `test()`, `test.step()`, `test.describe()`, хуки (beforeAll, beforeEach, afterEach, afterAll)

📁 [Документація](./docs/01-playwright-overview/README.md) | [Приклади](./docs/01-playwright-overview/examples/)

---

### Модуль 02: Config file та просунута конфігурація

**Конфігураційний файл Playwright**

- Призначення `playwright.config.ts` (.js)
- Основні опції: testDir, fullyParallel, retries, workers, reporter, use
- testMatch / testIgnore — фільтрація тестових файлів
- Просунута конфігурація: timeout, outputDir, globalSetup, globalTeardown
- Projects: кілька браузерів, запуск одного проекту `--project=firefox`
- Розділення тестів на проекти (Smoke vs Default)

📁 [Документація](./docs/02-config-file/README.md) | [Приклади](./docs/02-config-file/examples/)

---

### Модуль 03: Browser, Context і Page. UI mode

**Фікстури та графічний режим**

- Фікстури: page, context, browser, request — що це й навіщо
- Ізоляція тестів за рахунок окремих context/page
- UI mode: `npx playwright test --ui` — запуск та перегляд тестів у графічному інтерфейсі

📁 [Документація](./docs/03-browser-context-page-ui/README.md) | [Приклади](./docs/03-browser-context-page-ui/examples/)

---

## Швидкий старт

### Передумови

- Node.js >= 18
- npm

### Встановлення та запуск тестів

1. Перейдіть у папку Playwright-проєкту:
   ```bash
   cd examples/test-frameworks/playwright
   ```

2. Встановіть залежності та браузери:
   ```bash
   npm install
   npx playwright install
   ```

3. Запустіть тести:
   ```bash
   npm run test
   ```

   Або відкрийте UI mode:
   ```bash
   npm run test:ui
   ```

4. Перегляньте HTML-звіт після запуску:
   ```bash
   npm run report
   ```

## Структура проєкту

```
Playwright_overview/
├── docs/                                    # Документація модулів
│   ├── 01-playwright-overview/              # Модуль 01: Playwright overview
│   │   ├── README.md
│   │   └── examples/
│   ├── 02-config-file/                      # Модуль 02: Config file
│   │   ├── README.md
│   │   └── examples/
│   └── 03-browser-context-page-ui/         # Модуль 03: Browser, Context, Page, UI mode
│       ├── README.md
│       └── examples/
├── examples/test-frameworks/playwright/     # Повноцінний Playwright-проєкт
│   ├── playwright.config.ts
│   ├── package.json
│   ├── README.md
│   └── tests/
│       ├── 01-playwright-overview/
│       ├── 02-config-file/
│       └── 03-browser-context-page-ui/
├── prezentations/                           # Презентації по темах
│   ├── 01_Playwright_overview.md
│   ├── 02_Config_file.md
│   └── 03_Browser_context_UiMode.md
├── README.md                                # Цей файл
├── LICENSE
└── .gitignore
```

## Як використовувати курс

### Для студентів

1. **Встановіть** — дотримуйтесь розділу «Швидкий старт».
2. **Вивчайте теми по черзі** — від Модуля 01 до Модуля 03.
3. **Читайте теоретичний матеріал** — кожна тема має README.md у `docs/XX-topic/`.
4. **Переглядайте презентації** — у папці `prezentations/`.
5. **Переглядайте приклади** — у `docs/XX-topic/examples/`.
6. **Запускайте тести** — у `examples/test-frameworks/playwright/` з коментарями українською.
7. **Спробуйте UI mode** — `npm run test:ui` у папці Playwright-проєкту.

### Рекомендована послідовність

1. **Модуль 01** — Playwright overview & installation
2. **Модуль 02** — Config file та просунута конфігурація
3. **Модуль 03** — Browser, Context, Page та UI mode

## Запуск прикладів

У папці `examples/test-frameworks/playwright/`:

```bash
# Усі тести (chromium, firefox, webkit)
npm run test

# UI mode (графічний інтерфейс)
npm run test:ui

# Тести за модулями
npm run test:module1   # Модуль 01
npm run test:module2   # Модуль 02
npm run test:module3   # Модуль 03

# Один браузер
npm run test:chromium

# Перегляд HTML-звіту
npm run report
```

Через npx з кореня репозиторію:

```bash
cd examples/test-frameworks/playwright
npx playwright test
npx playwright test --ui
npx playwright test --project=firefox
npx playwright show-report
```

## Для кого цей курс

- Початківці в автоматизованому тестуванні
- Студенти курсів з QA/тестування
- Розробники, які хочуть навчитися Playwright
- QA-інженери, які переходять на Playwright

## Додаткові ресурси

- [Playwright Documentation](https://playwright.dev/docs/intro)
- [Playwright Test Configuration](https://playwright.dev/docs/test-configuration)
- [Playwright UI Mode](https://playwright.dev/docs/test-ui-mode)

## Ліцензія

MIT License — дивіться файл LICENSE.
